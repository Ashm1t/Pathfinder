# Core agent
ollama>=0.3.0            # local LLM client (hot-swappable; vLLM via OpenAI-compat later)
watchdog>=4.0.0         # filesystem change events (replaces polling)

# Document text extraction (fixes the C++ backend's binary-file gap)
python-docx>=1.1.0      # .docx
pypdf>=4.0.0            # .pdf  (maintained successor to PyPDF2)
openpyxl>=3.1.0         # .xlsx (registers, seizure lists)

# IPC to the C++ HUD
fastapi>=0.110.0
uvicorn>=0.29.0

# MCP client (standard servers, run locally)
mcp>=1.0.0

# Optional — semantic retrieval (deferred; only if "related cases" is needed)
# chromadb>=0.5.0
# sentence-transformers>=3.0.0

# stdlib used: sqlite3, threading, dataclasses, re, datetime
